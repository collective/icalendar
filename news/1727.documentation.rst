Rewrote the ``COMMENT`` and ``CONTACT`` property docstrings in :mod:`icalendar.attr` to follow the project's Python docstring style guide, replacing copy-pasted RFC text with Pythonic summaries, ``:rfc:`` cross-references, and runnable examples. These properties are shared by ``Event``, ``Todo``, ``Journal``, ``Calendar``, and the availability components, so a single change improves many tracked docstring items at once. Prepared with AI assistance; reviewed and tested locally. @ranjanssgj

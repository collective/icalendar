Fixed broken cross-references to private functions in ``icalendar.parser.string`` by rendering them as code instead of unresolved links. AI-assisted: GPT-5.6 Sol was used to review the broken Sphinx references. The final change was manually verified. @josemmiranda04

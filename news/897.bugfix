Retyped the shared start/end/duration helpers in :mod:`icalendar.attr` to accept ``Event`` or ``Todo`` instead of ``Component``, so type checkers no longer treat them as accepting arbitrary ``Component`` instances. Replaced a narrowing assert with an explicit error. Runtime behavior for valid inputs is unchanged. Drafted with Claude Sonnet 5, reviewed by me. @SemTiOne

Migrated the CI matrix tests to the three-argument event path, added ``pull_request_review`` PyPy cases in tests, and simplified the logic for when PyPy is tested in CI without changing that it runs only on a push to ``main``. Drafted with Claude Sonnet 5, reviewed by me. @SemTiOne

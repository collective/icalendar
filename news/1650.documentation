``single_utc_property`` no longer composes part of each property's docstring from the property name. Every affected property (``DTSTAMP``, ``CREATED``, ``LAST_MODIFIED``, ``ACKNOWLEDGED``, ``X-MOZ-SNOOZE-TIME``, ``X-MOZ-LASTACK``, ``rfc_7953_dtstart_property``, ``rfc_7953_dtend_property``) now carries one complete static docstring at its call site, so the summary line describes the property instead of a generated sentence with mismatched indentation. ``DTSTAMP`` is no longer aliased to ``stamp``, yet functions the same. Part of #1650. I used AI to assist me with this change. @Morningstar202604
